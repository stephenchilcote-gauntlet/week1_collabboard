{
  "name": "collabboard-ai-proxy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.18",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "langfuse": "^3.38.6"
  }
}
